# Base URL para a API no dev. Deixe em branco para usar o proxy do Vite.
VITE_API_BASE_URL=

# Alvo do proxy do Vite para dev (o navegador não acessa portas inseguras diretamente)
VITE_DEV_API_PROXY_TARGET=http://localhost:6000
